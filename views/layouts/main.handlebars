<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Rent Reviewer</title>
  <link rel="stylesheet" href="/stylesheets/style.css" />
</head>

<body>
  <div class="wrapper">
    <nav>
      <h2>
        {{#if loggedIn}}
        <button>Make a review</button>>
        {{/if}}
        <a href="/" class="logo">Rent Reviewer</a>
      </h2>
      <ul>
        <li>
          {{#if loggedIn}}
          <button id="logout">logout</button>
          <h2>You Are Logged In</h2>
          {{else}}
          <a href="/dashboard">Dashboard</a>
          {{/if}}

        </li>
        <li>
          {{#if loggedIn}}
          <a href="/dashboard">dashboard</a>
          <button id="logout">logout</button>
          {{else}}
          <a href="/login">Login</a>
          {{/if}}
        </li>
      </ul>
    </nav>
      {{{body}}}



    <div class="footer">
      <h2>Made by Ava S, Jonny T, Kal O, and Susan B</h2>
      <p>
        &copy; 2022
    </div>

    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}

</body>

</html>