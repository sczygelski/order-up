<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Rent Reviewer</title>
  <link rel="stylesheet" href="/stylesheets/style.css" />
</head>

<body>
  <div class="wrapper">
    <nav>
      <h2>
        {{#if loggedIn}}
        <button>Make a review</button>>
        {{/if}}
        <a href="/api/review-routes.js">Rent Reviewer</a>
      </h2>
      <ul>
        <li>
          {{#if loggedIn}}
          <button id="logout" class="btn-no-style">logout</button>
          {{else}}
          <a href="/login">use other login button</a>
          {{/if}}
          <a href="/signup">Sign up/Logout</a>
        </li>
        <li>
          {{#if loggedIn}}
          <a href="/dashboard">dashboard</a>
          <button id="logout" class="btn-no-style">logout</button>
          {{else}}
          <a href="/login">Login</a>
          {{/if}}
        </li>
      </ul>
    </nav>

    <main>
      <div class="card-container-row">
        <div class="card-container-column">
          <div class="card">
            <h5>Read Reviews</h5>
            <div class="instructions">
              <p>Click here to read a review of other rental properties</p>
            </div>
          </div>

          <div class="card">
            <h5>Write a Review</h5>
            <div class="instructions">
              <p>Click here to write a review of your last rental experience</p>
            </div>
          </div>
        </div>

        <div class="card card-big">
          <img src="/images/homepagehouse.jpg"></img>
        </div>
      </div>
      {{{body}}}
    </main>


    <div class="footer">
      <h2>Made by Ava S, Jonny T, Kal O, and Susan B</h2>
      <p>
        &copy; 2022
    </div>

    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}

</body>

</html>